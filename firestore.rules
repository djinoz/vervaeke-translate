rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Conservative starter posture for the scaffold.
    // Tighten this once the concrete collections and trusted mutation paths exist.

    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
